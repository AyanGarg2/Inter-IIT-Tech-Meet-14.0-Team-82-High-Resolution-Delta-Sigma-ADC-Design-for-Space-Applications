# ============================================================================
# Environment Configuration File
# Team_82 - Inter IIT Tech Meet 14.0 | ISRO-VLSI
# ============================================================================

# ----------------------------------------------------------------------------
# OPERATING SYSTEM INFORMATION
# ----------------------------------------------------------------------------
OS_NAME="Windows 11 Home Single Language"
OS_VERSION="10.0.26200.7309"
OS_ARCHITECTURE="64-bit"

# ----------------------------------------------------------------------------
# MATLAB/SIMULINK ENVIRONMENT
# ----------------------------------------------------------------------------
MATLAB_VERSION_PRIMARY="R2024a"
MATLAB_VERSION_SECONDARY="R2024b"
SIMULINK_VERSION_PRIMARY="24.1"
SIMULINK_VERSION_SECONDARY="24.2"

# MATLAB Installation Paths (Update these to your actual installation paths)
MATLAB_ROOT="C:/Program Files/MATLAB/R2024a"
MATLAB_TOOLBOX_PATH="${MATLAB_ROOT}/toolbox"

# Required Toolboxes
MATLAB_REQUIRED_TOOLBOXES="DSP System Toolbox,Delta Sigma Toolbox,Mixed-Signal Blockset"

# ----------------------------------------------------------------------------
# HDL SIMULATION TOOLS
# ----------------------------------------------------------------------------
SIMULATOR_NAME="Vivado"
VIVADO_VERSION="2024.1"
VIVADO_HOME="C:/Xilinx/Vivado/2024.1"
HDL_LANGUAGE="Verilog 2001"
WAVEFORM_VIEWER="Vivado"

# Vivado Paths
VIVADO_BIN="${VIVADO_HOME}/bin"
VIVADO_LIB="${VIVADO_HOME}/lib/win64.o"

# ----------------------------------------------------------------------------
# SYNTHESIS TOOLS
# ----------------------------------------------------------------------------
SYNTHESIS_TOOL="Cadence Genus"
GENUS_VERSION="23.1"
GENUS_HOME="/path/to/cadence/genus/23.1"
SYNTHESIS_OPTIMIZATION="area_speed"
TARGET_CLOCK_FREQUENCY="8.192MHz"

# Cadence Genus Paths
GENUS_BIN="${GENUS_HOME}/bin"
GENUS_LIB="${GENUS_HOME}/lib"

# ----------------------------------------------------------------------------
# PLACE & ROUTE (BACKEND) TOOLS
# ----------------------------------------------------------------------------
PNR_TOOL="Cadence Innovus"
INNOVUS_VERSION="21.1"
INNOVUS_HOME="/path/to/cadence/innovus/21.1"
LAYOUT_FORMAT="GDSII"

# Cadence Innovus Paths
INNOVUS_BIN="${INNOVUS_HOME}/bin"
INNOVUS_LIB="${INNOVUS_HOME}/lib"

# ----------------------------------------------------------------------------
# PROCESS DESIGN KIT (PDK)
# ----------------------------------------------------------------------------
PDK_NAME="UMC 90nm"
TECHNOLOGY_NODE="90nm CMOS"
PDK_HOME="/path/to/pdk/umc90nm"

# PDK Library Paths
PDK_LIBRARY_PATH="${PDK_HOME}/libs"
PDK_TECH_FILE="${PDK_HOME}/tech/tech.tf"
PDK_LEF_PATH="${PDK_HOME}/lef"
PDK_GDS_PATH="${PDK_HOME}/gds"

# ----------------------------------------------------------------------------
# CADENCE COMMON SETTINGS
# ----------------------------------------------------------------------------
CADENCE_HOME="/path/to/cadence"
CDS_AUTO_64BIT="ALL"
CDS_LIC_FILE="5280@license_server"

# ----------------------------------------------------------------------------
# LICENSE SERVER CONFIGURATION
# ----------------------------------------------------------------------------
LM_LICENSE_FILE="27000@license_server:5280@license_server"
XILINX_LICENSE_FILE="2100@license_server"

# ----------------------------------------------------------------------------
# SYSTEM PATH CONFIGURATION
# ----------------------------------------------------------------------------
# Add tool binaries to PATH
# PATH="${VIVADO_BIN}:${GENUS_BIN}:${INNOVUS_BIN}:${PATH}"

# Library paths (Linux/Unix)
# LD_LIBRARY_PATH="${VIVADO_LIB}:${GENUS_LIB}:${INNOVUS_LIB}:${PDK_LIBRARY_PATH}:${LD_LIBRARY_PATH}"

# Library paths (Windows)
# PATH="${VIVADO_LIB};${GENUS_LIB};${INNOVUS_LIB};${PDK_LIBRARY_PATH};${PATH}"

# ----------------------------------------------------------------------------
# PROJECT CONFIGURATION
# ----------------------------------------------------------------------------
PROJECT_NAME="Team_82_ISRO_VLSI"
PROJECT_ROOT="$(pwd)"
DESIGN_ROOT="${PROJECT_ROOT}/design"
SIMULATION_ROOT="${PROJECT_ROOT}/simulation"
SYNTHESIS_ROOT="${PROJECT_ROOT}/synthesis"
LAYOUT_ROOT="${PROJECT_ROOT}/layout"
RESULTS_ROOT="${PROJECT_ROOT}/results"

# ----------------------------------------------------------------------------
# PYTHON ENVIRONMENT
# ----------------------------------------------------------------------------
PYTHON_VERSION="3.x"
PYTHON_PACKAGES="numpy,matplotlib"
PYTHONPATH="${PROJECT_ROOT}/scripts:${PYTHONPATH}"

# ----------------------------------------------------------------------------
# DESIGN PARAMETERS
# ----------------------------------------------------------------------------
CLOCK_FREQUENCY="8.192"
CLOCK_UNIT="MHz"
SUPPLY_VOLTAGE="1.0V"
TEMPERATURE="25C"

# ----------------------------------------------------------------------------
# LOGGING AND DEBUG
# ----------------------------------------------------------------------------
LOG_LEVEL="INFO"
DEBUG_MODE="false"
LOG_DIR="${PROJECT_ROOT}/logs"

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
# 1. Update all /path/to/* entries with actual installation paths
# 2. Adjust license server addresses and ports for your organization
# 3. For Windows systems, use semicolons (;) for PATH separators
# 4. For Linux/Unix systems, use colons (:) for PATH separators
# 5. Source this file: source .env (Linux/Mac) or use a .env loader (Windows)
#
# Last Updated: $(date)
# ============================================================================